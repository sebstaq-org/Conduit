# Provider Scenario Fixtures

These fixtures are curated E2E scenario inputs for provider-level replay through
`service-bin --provider-fixtures`. They are synthetic, public-safe failure and
state-shape fixtures, not raw provider captures.

Use these only when the test needs a deterministic provider behavior that is
not represented by the default happy-path parity fixtures.
