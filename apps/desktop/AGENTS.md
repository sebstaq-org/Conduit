# Desktop Shell Guidance

This directory is reserved for Electron shell and desktop integration only.

- Do not add shared feature logic here.
- Do not add shared UI primitives here.
- Future shared capability behavior belongs in `@conduit/app-client` or `@conduit/app-core`.
- Raw desktop primitives and Electron integration belong here only when shell work actually begins.
