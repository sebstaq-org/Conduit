# Testdata

`providers/` is reserved for real provider protocol captures, `failures/` for transport or failure fixtures, and `golden/` for Conduit-owned read-side projections. Phase 0.5 creates the roots only so Phase 1 evidence can land without new structure churn.
