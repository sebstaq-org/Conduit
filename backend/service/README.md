# backend/service

This Rust workspace is present in Phase 0.5 only to reserve the final crate layout, pin the Rust toolchain, and keep boundary names stable. No ACP host or provider runtime behavior is implemented yet.
