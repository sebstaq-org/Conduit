# Artifacts

Manual proof captured during verification is archived outside the repo under `/srv/devops/repos/conduit-artifacts/manual/`; `artifacts/manual/` is kept only as an empty guarded placeholder. `artifacts/automated/` is for generated CI or scripted proof that is explicitly meant to stay in the repo. Runtime source code does not belong here.
